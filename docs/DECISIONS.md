# DECISIONS.md

이 문서는 프로젝트 진행 중 **AI가 제안한 초안을 어떻게 검토하고, 왜 그렇게 결정했는지**를 기록하기 위한 로그입니다.
포트폴리오/면접에서 "이 부분 AI가 짰나요?"라는 질문에 근거 있게 답하기 위한 용도이므로, 짧더라도 결정할 때마다 바로바로 기록하는 것을 권장합니다.

**작성 원칙**
- 코드를 다 짜고 나서 몰아서 쓰지 말고, 결정하는 그 순간에 3~5줄이라도 남기세요.
- "왜 안 했는지"도 기록 대상입니다 (예: 승인제 예약은 왜 이번 스코프에서 뺐는지).
- AI 제안을 그대로 채택했더라도, "왜 그대로 채택했는지" 한 줄은 남기세요. 무비판적 수용과 검토 후 채택은 다릅니다.

---

## 사용 방법

아래 `## DEC-001` 형식으로 번호를 이어가며 항목을 추가합니다. 각 필드는 다음을 의미합니다.

| 필드 | 설명 |
|---|---|
| **배경/문제** | 어떤 상황에서 이 결정이 필요했는가 |
| **AI 초안** | AI(Claude)가 처음 제안한 내용 (프롬프트 요약 + 결과 요약) |
| **검토 포인트** | 그 제안을 보고 무엇을 의심하거나 확인했는가 |
| **최종 결정** | 실제로 채택한 내용 (AI 제안과 다르다면 무엇이 달라졌는지 명시) |
| **결정 이유 / 트레이드오프** | 왜 이렇게 결정했는지, 포기한 대안은 무엇인지 |
| **상태** | `채택` / `보류` / `기각` / `추후 재검토` |
| **관련 파일/커밋** | 해당 결정이 반영된 코드 위치 (구현 후 채워도 됨) |

---

## DEC-001: (예시) 예약 홀드 유지시간

- **날짜**: 2026-08-31
- **배경/문제**: 결제 진행 중인 재고를 얼마나 오래 잡아둘지 정해야 함. 너무 길면 실사용 가능 재고가 줄고, 너무 짧으면 결제 중 만료되는 사용자가 생김.
- **AI 초안**: PRD/스키마 설계 시 Claude가 기본값 10분을 제안 (근거: 일반적인 PG 결제 소요시간 고려).
- **검토 포인트**:
  - 실제 PG 결제창 진입~완료까지 평균 소요시간이 어느 정도인지 확인 필요
  - 홀드 시간이 길수록 "가짜 매진"처럼 보이는 문제가 커질 수 있음
- **최종 결정**: (직접 결정한 값을 기입 — 예: 10분 유지 / 7분으로 단축 등)
- **결정 이유 / 트레이드오프**: (예: 캠핑장 특성상 결제수단 선택이 오래 걸리는 경우가 있어 10분 유지. 대신 홀드 5분 경과 시 배너로 재확인 유도하기로 함)
- **상태**: 채택
- **관련 파일/커밋**: `lib/reservations/hold.ts`, `supabase/migrations/xxxx_reservations.sql`

---

## DEC-002: 환불 규정 상세 기준

- **날짜**: 2026-09-07
- **배경/문제**: `예약결제시스템_DB스키마_API설계.md` 6장에 "환불 규정 상세 기준"이 미정 상태로 남아 있었고, `refund_policies` 테이블(체크인 D-n일 전 → 환불%)에 넣을 실제 값이 필요했음. 클라이언트 확인 전이라 사용자가 임의 결정을 위임함.
- **AI 초안**: Claude가 국내 숙박업 환불규정 관행을 참고해 5단계 계단식 비율 제안: D-7 이상 100%, D-5 70%, D-3 50%, D-1 30%, 당일(D-0)/노쇼 0%.
- **검토 포인트**: 실제 계약/약관에 따라 달라질 수 있는 값이므로, 이 값은 "보일러플레이트 기본값"이며 프로젝트별로 `refund_policies` 테이블 데이터만 교체하면 되도록 하드코딩하지 않고 테이블화했는지 확인.
- **최종 결정**: 위 5단계 비율을 그대로 채택, `supabase/seed.sql`에 반영. 매칭 로직은 "취소 시점 기준 남은 일수 이하의 `days_before` 중 가장 큰 값"을 찾아 적용 (예: D-4 취소 → D-3 규정 50% 적용).
- **결정 이유 / 트레이드오프**: 규정을 코드가 아닌 `refund_policies` 테이블 데이터로만 관리하므로, 실제 클라이언트 확정 값이 나오면 마이그레이션 변경 없이 데이터만 갱신하면 됨. 값 자체는 임의 기본값이라 실서비스 적용 전 반드시 사업주 확인 필요.
- **상태**: 채택 (기본값, 실서비스 전 재검토 필요)
- **관련 파일/커밋**: `supabase/migrations/20260907000001_init_schema.sql`(refund_policies), `supabase/seed.sql`

---

## DEC-003: 객실/사이트별 재고 수량 및 초기 데이터

- **날짜**: 2026-09-07
- **배경/문제**: `rooms`(개별 유닛) 테이블과 `room_types` 카탈로그에 넣을 샘플 데이터가 없어 예약 플로우(홀드/동시성 테스트 포함)를 개발·테스트할 수 없었음.
- **AI 초안**: 펜션+캠핑장이 섞인 보일러플레이트 컨셉에 맞춰 room_type 3종(디럭스 온돌룸/스탠다드 온돌룸/A구역 오토캠핑 사이트) × 재고 5/8/10개(총 23유닛) 제안.
- **검토 포인트**: 재고 수가 너무 적으면 동시성(EXCLUDE 제약) 테스트 시나리오가 단조로워지고, 너무 많으면 샘플 데이터 관리가 번거로움 → 두 자릿수 초반대가 적당하다고 판단.
- **최종 결정**: property 1개("샘플 펜션") 하위에 room_type 3종, 총 23개 room 유닛 생성. `supabase/seed.sql`에 `generate_series`로 반영.
- **결정 이유 / 트레이드오프**: 실제 상호명/객실명/사진 등은 프로젝트별 커스터마이징 대상(PRD 4-2)이므로 이름은 "샘플 펜션" 등 플레이스홀더로 명시해 실브랜딩과 혼동되지 않게 함.
- **상태**: 채택 (플레이스홀더 데이터, fork 시 교체 전제)
- **관련 파일/커밋**: `supabase/seed.sql`

---

## DEC-004: 성수기/주말 등 요금 정책(price_rules) 예시값

- **날짜**: 2026-09-07
- **배경/문제**: `price_rules` 스키마(요일 기반 vs 특정기간 기반, priority)는 설계돼 있었지만 실제 가격 예시값이 없어 FR-1(가격 자동 반영) 확인이 불가능했음.
- **AI 초안**: room_type별로 기준가(base_price, 비수기 평일) 대비 주말(금·토) 요금과 성수기(여름 7/15~8/24, 연말연시 12/24~1/2) 특정기간 요금을 각각 정의하고, 성수기 규칙의 `priority`를 주말 규칙보다 높게(10 vs 1) 설정해 성수기 기간에는 요일과 무관하게 성수기가를 우선 적용.
- **검토 포인트**: 두 규칙이 같은 날짜에 동시에 매칭될 수 있어(성수기 기간의 주말) `priority` 우선순위 로직이 반드시 필요함을 재확인 — 가격 조회 로직(`lib/reservations` 또는 API)에서 "매칭되는 price_rules 중 priority 최댓값" 선택 규칙으로 구현할 것.
- **최종 결정**: 디럭스 온돌룸 100,000/130,000/180,000(연말 160,000)원, 스탠다드 온돌룸 70,000/90,000/120,000(연말 110,000)원, 캠핑사이트 40,000/55,000/80,000(연말 70,000)원(순서: 평일/주말/여름성수기(연말성수기))을 `supabase/seed.sql`에 반영.
- **결정 이유 / 트레이드오프**: 실제 요금은 사업주 확인 필요한 값이라 임의 기본값임을 명시. 다만 "요일 규칙과 기간 규칙이 겹칠 때 priority로 해소한다"는 알고리즘 자체는 스키마 설계 의도를 그대로 따른 것이라 향후 실제 값 교체 시에도 로직 변경이 필요 없음.
- **상태**: 채택 (기본값, 실서비스 전 재검토 필요)
- **관련 파일/커밋**: `supabase/migrations/20260907000001_init_schema.sql`(price_rules), `supabase/seed.sql`

---

## DEC-005: PaymentProvider 인터페이스를 PortOne V2 실제 흐름에 맞게 재설계

- **날짜**: 2026-09-09
- **배경/문제**: `lib/payments/PaymentProvider.ts`는 PG사 선정(DEC: 토스페이먼츠/PortOne 경유) 이전에 `requestPayment(서버가 결제를 요청해 paymentKey를 받는 방식)` 형태로 미리 스캐폴딩되어 있었음. M3 착수 시 PortOne V2 공식 문서(서버 SDK `@portone/server-sdk`, 브라우저 SDK `@portone/browser-sdk`)를 직접 확인해보니, PortOne V2는 결제 요청 자체를 브라우저 SDK가 클라이언트에서 직접 처리하고(`PortOne.requestPayment()`), 서버는 그 결과를 절대 그대로 신뢰하지 말고 `paymentId`로 PG API를 재조회해 검증해야 하는 구조였음(서버발 "요청"이 없음).
- **AI 초안**: Claude가 WebSearch/WebFetch로 `portone-io/server-sdk`, `@portone/browser-sdk` 공식 문서·타입 정의를 조사한 뒤, 기존 `requestPayment`를 제거하고 `verifyPayment(orderId)`(PG 조회/검증) 중심으로 인터페이스를 재설계할 것을 제안.
- **검토 포인트**: 인터페이스를 사후에 바꾸는 게 맞는지 vs 기존 시그니처를 억지로 맞추는 게 나은지 검토 — 기존 시그니처(`paymentKey`/`redirectUrl`)로는 PortOne V2 흐름을 표현할 수 없어 억지로 맞추면 오히려 오해를 유발한다고 판단. PRD FR-5의 "PG사 교체 가능한 인터페이스"라는 의도 자체는 유지하면서, 실제 구현 가능한 형태로 고치는 것이 M3의 목적에 맞다고 결론.
- **최종 결정**: `PaymentProvider`를 `verifyPayment(orderId)` / `verifyWebhook(rawBody, headers)` / `refund(req)` 3개 메서드로 재정의. 결제 확정 로직(`lib/reservations/payment.ts`의 `completePayment`)은 "브라우저 성공 응답 직후 클라이언트가 호출하는 sync 경로"와 "PG 웹훅" 양쪽에서 재사용하며, `payments.order_id`(UNIQUE) 기준으로 멱등하게 동작하도록 구현(FR-6).
- **결정 이유 / 트레이드오프**: 브라우저 SDK 응답은 클라이언트에서 위변조 가능하므로 서버 재검증이 필수이며, 이 재검증 로직을 "결제 확정" 도메인 함수 하나로 통일해 sync/webhook 두 경로가 로직을 중복 구현하지 않게 함. 다만 verifyWebhook의 두 번째 인자를 문자열 서명 하나에서 헤더 레코드(`webhook-id`/`webhook-signature`/`webhook-timestamp`)로 바꿔 PortOne의 실제 웹훅 서명 방식(Svix 스타일)에 맞춤 — 다른 PG사를 붙일 때 이 헤더 인터페이스가 안 맞을 수 있다는 점은 알려진 한계로 남김.
- **상태**: 채택
- **관련 파일/커밋**: `lib/payments/PaymentProvider.ts`, `lib/payments/PortoneProvider.ts`, `lib/reservations/payment.ts`, `app/api/reservations/[id]/payment/complete/route.ts`, `app/api/payments/webhook/[provider]/route.ts`

---

## DEC-006: 비회원 예약 취소/홀드해제 API의 소유자 검증 누락 보완

- **날짜**: 2026-09-10
- **배경/문제**: [ReservationLookupForm.tsx](../components/reservations/ReservationLookupForm.tsx)는 예약자명+전화번호로 조회할 때만 본인 확인을 거치는데, 실제 취소를 수행하는 `POST /reservations/:id/cancel`과 `DELETE /reservations/:id/hold`는 URL의 예약 UUID만으로 동작하고 요청자가 실제 예약자인지 재검증하지 않았음. 예약 UUID가 브라우저 히스토리, 결제 완료 페이지 URL 등으로 노출될 경우 조회 단계를 거치지 않고 타인의 예약을 취소할 수 있는 IDOR 성격의 문제.
- **AI 초안**: Claude가 "PortOne 같은 외부 본인인증 API 연동이 필요한가?"라는 질문에, 이 시스템은 비회원 예약 조회 로직(예약자명+전화번호 대조)을 이미 갖추고 있으므로 실명 본인인증 API 없이도 동일한 방식(소유자명+전화번호 매칭)을 취소/홀드해제 API에도 적용하면 된다고 제안.
- **검토 포인트**: 관리자 강제취소(`PATCH /admin/reservations/:id/cancel`)는 Supabase Auth 세션으로 이미 별도 인증되므로, `cancelReservation` 함수에 검증을 필수로 넣으면 관리자 경로가 깨진다는 점을 확인 → 고객 경로에서만 선택적으로 켜지도록 옵션(`verifyGuest`)으로 설계.
- **최종 결정**: `lib/reservations/cancel.ts`의 `previewRefund`(환불액 미리보기)와 `cancelReservation`(결제 완료 후 실제 취소, `CancelOptions.verifyGuest`)에는 `guest: { name, phone }` 검증을 적용해 예약 레코드와 불일치 시 `GUEST_MISMATCH`(403)로 거부. 프론트(`ReservationLookupForm.tsx`)는 조회 폼에 입력된 값을 이 요청들에 실어 보냄.
  - **1차 수정 후 재검토(같은 날)**: 1차 구현에서는 `releaseHold`(결제 전 HOLD 해제)에도 동일하게 guest 필수 검증을 걸었는데, `/checkout/:reservationId` 페이지(예약 생성 직후 같은 세션에서 바로 접근하는 결제 대기 화면)의 "예약 취소하고 돌아가기" 버튼이 이 API를 이름/전화번호 없이 호출하고 있어 취소가 막히는 회귀가 발생함(사용자 리포트로 발견). 재검토 결과 HOLD는 결제 전 상태라 취소돼도 돈이 오가지 않고 재고만 풀리는 수준이라 오용 시 피해가 "홀드 가로채기 취소(경미한 방해)" 정도이며, 어차피 24시간 후 자동 만료되어 파급력이 제한적임. 반면 체크아웃 페이지는 재입력 UI를 새로 만들 자리가 없는 즉시 접근 플로우라, 여기에 강제하면 정상 취소 흐름 자체가 깨짐. → `releaseHold`의 guest 인자를 선택(optional)으로 되돌려, 값이 오면 검증하고 없으면 통과시키도록 수정. 실제 돈이 움직이는 `cancelReservation`(결제 완료 후 취소)만 검증을 유지.
- **결정 이유 / 트레이드오프**: 이미 비회원 조회에 쓰던 소유자 확인 방식을 재사용해 새 외부 연동(PortOne 본인인증 등) 없이 구멍을 메움. 다만 이 방식은 실명 인증이 아니라 "예약 시 입력한 정보와 일치하는가"만 확인하는 수준이라, 이름+전화번호 조합을 아는 제3자는 여전히 (결제 완료된) 예약을 취소할 수 있음 — 보일러플레이트 스코프에서는 허용 가능한 트레이드오프로 판단했고, 더 강한 보안이 필요하면 조회 시 단기 서명 토큰을 발급해 재사용하는 방식으로 추후 교체 가능. HOLD 해제는 금전적 위험이 없다고 판단해 검증을 의도적으로 생략함 — 만약 향후 HOLD 단계에서도 민감 정보를 노출하게 되거나 악용 사례가 확인되면 재검토 필요.
- **상태**: 채택
- **관련 파일/커밋**: `lib/reservations/cancel.ts`, `app/api/reservations/[id]/cancel/route.ts`, `app/api/reservations/[id]/hold/route.ts`, `components/reservations/ReservationLookupForm.tsx`, `components/reservation/CheckoutPanel.tsx`(영향 확인, 코드 변경 없음)

---

## 참고: 기록해두면 특히 유용한 결정 포인트 목록

프로젝트 진행하면서 아래 항목들은 거의 반드시 결정을 거치게 되니, 마주칠 때마다 DEC 항목으로 남겨보세요.

- [ ] 예약 홀드 유지시간
- [ ] 이중예약 방지 방식 (DB 제약 vs 애플리케이션 락 vs 둘 다) — 왜 `EXCLUDE` 제약을 선택했는지
- [ ] PG사 선정 및 연동 방식 (추상화 인터페이스 설계 방향)
- [ ] 환불 규정을 하드코딩할지 테이블로 뺄지
- [x] 비회원 예약 조회 인증 방식 (예약번호+전화번호 조합의 보안 트레이드오프) → DEC-006
- [ ] 웹훅 멱등성 처리 방식 (order_id unique 제약 등)
- [ ] 스코프에서 제외한 기능과 그 이유 (승인제 예약, 다국어, 회원시스템 등)
- [ ] 배치(Cron) 주기를 1분으로 정한 이유와 한계
